<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Criar Treino</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>

        .card-header {
            background-color: #000;
            color: #fff;
        }

        .btn-danger {
            background-color: #c40000;
            border-color: #c40000;
        }

        .btn-danger:hover {
            background-color: #a30000;
            border-color: #a30000;
        }
    </style>
</head>
<body>

<div class="container mt-5 mb-5">

    <h3 class="mb-4 fw-bold text-danger">
        Criar treino personalizado
    </h3>

    <div class="card shadow-sm mb-4">
        <div class="card-header">
            Informações do aluno
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-6">
                    <p><strong>Email:</strong> <%= aluno.email %></p>
                    <p><strong>Objetivo:</strong> <%= aluno.objetivo %></p>
                </div>
                <div class="col-md-6">
                    <p><strong>Peso:</strong> <%= aluno.peso_atual %> kg</p>
                    <p><strong>Altura:</strong> <%= (aluno.altura / 100).toFixed(2) %> m</p>
                </div>
            </div>

            <hr>

            <p class="text-danger">
                <strong>Restrições:</strong>
                <%= aluno.restricoes || 'Nenhuma' %>
            </p>
        </div>
    </div>

    <div class="card shadow-sm">
        <div class="card-header">
            Criar novo treino
        </div>
        <div class="card-body">

            <form method="POST" action="/instrutor/treino/criar/<%= aluno.id_aluno %>">

                <div class="mb-3">
                    <label class="form-label fw-semibold">Nome do treino</label>
                    <input type="text" name="nome" class="form-control" placeholder="Ex: Treino A - Superior" required>
                </div>

                <div class="mb-3">
                    <label class="form-label fw-semibold">Descrição</label>
                    <textarea name="descricao" class="form-control" rows="3"
                        placeholder="Descrição geral do treino"></textarea>
                </div>

                <div class="mb-4">
                    <label class="form-label fw-semibold">Dia da semana</label>
                    <select name="dia_semana" class="form-select" required>
                        <option value="">Selecione</option>
                        <option value="SEGUNDA">Segunda</option>
                        <option value="TERCA">Terça</option>
                        <option value="QUARTA">Quarta</option>
                        <option value="QUINTA">Quinta</option>
                        <option value="SEXTA">Sexta</option>
                        <option value="SABADO">Sábado</option>
                        <option value="DOMINGO">Domingo</option>
                    </select>
                </div>

                <div class="mb-4">
                    <label class="form-label fw-semibold">Observações</label>
                    <textarea name="observacoes" class="form-control" rows="3"
                        placeholder=""></textarea>
                </div>

                <div class="d-grid">
                    <button class="btn btn-danger btn-lg">
                        Criar treino
                    </button>
                </div>

            </form>

        </div>
    </div>

</div>

</body>
</html>

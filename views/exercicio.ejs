<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title><%= exercicio.nome %> | Detalhes</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        .card-zoom {
            transition: transform .3s ease, box-shadow .3s ease;
        }
        .card-zoom:hover {
            transform: scale(1.03);
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
        }
        .media-box img, .media-box video {
            max-width: 100%;
            border-radius: 12px;
        }
    </style>
</head>

<body class="bg-light">

<div class="container mt-5">

    <a href="javascript:history.back()" class="btn btn-secondary mb-3">⬅ Voltar</a>

    <div class="card shadow card-zoom">
        <div class="card-body">

            <h2 class="text-success mb-3"><%= exercicio.nome %></h2>

            <div class="row">


                <div class="col-md-6 media-box mb-3">
                    <% 
                    const url = exercicio.url;
                    const isYoutube = url && (url.includes("youtube.com") || url.includes("youtu.be"));
                    %>

                    <% if (exercicio.tipo_midia === 2) { %>

                        <img src="<%= url %>" class="img-fluid rounded" alt="<%= exercicio.nome %>">

                    <% } else if (exercicio.tipo_midia === 1 && isYoutube) { %>
                        <iframe width="100%" height="315"
                            src="https://www.youtube.com/embed/<%= 
                                url.includes('v=') 
                                ? url.split('v=')[1].split('&')[0] 
                                : url.split('/').pop()
                            %>"
                            title="<%= exercicio.nome %>"
                            frameborder="0"
                            allowfullscreen>
                        </iframe>

                    <% } else { %>
                        <p class="text-muted">Nenhuma mídia cadastrada.</p>
                    <% } %>

                </div>

                <div class="col-md-6">
                    <h5 class="text-success">Descrição</h5>
                    <p><%= exercicio.descricao %></p>

                    <hr>

                    <h5 class="text-success">Parâmetros recomendados</h5>
                    <ul>
                        <li><strong>Séries:</strong> <%= exercicio.serie_padrao %></li>
                        <li><strong>Repetições:</strong> <%= exercicio.repeticoes_padrao %></li>
                        <li><strong>Descanso:</strong> <%= exercicio.descanso_padrao %> segundos</li>
                    </ul>
                </div>

            </div>

        </div>
    </div>

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>